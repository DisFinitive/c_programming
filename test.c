#include <stdio.h>

int main(void) {

    char ch;
    ch = 'd';

    float d;
    d = 15.5f;

    int value;
    int _index = 34;
    value = 34;

    // printf("ch = %c, code = %d\n", ch, ch);

    int size = sizeof(d);

    // printf("size float = %d", size);

    int a, b;

    a = '/';
    b = 47;
    
    // printf("a = %d, b = %d", a, b);

    int value1 = getchar();

    return 0;
}